S = header* data+ trailing?
header = #"[^\r\n]"+ linebreak
data = #"[^\s]"+ tab number tab number (tab field)* linebreak
tab = "\t"
number = #"[0-9]+"
field = #'[^\t\r\n"]+'
linebreak = #"(?:\r\n|\r|\n)"
trailing = #"[\s\S]*"
